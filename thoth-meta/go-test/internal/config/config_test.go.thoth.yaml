locator: internal/config/config_test.go
meta:
  architecture_notes: Uses temp cue fixtures to exercise loader and validator together, mirroring real CLI config consumption.
  function_list:
    - TestLoadBuildArtifactTargetSuffixFalseWithMultipleTargets
    - TestLoadBuildMainPackageOverride
    - TestLoadDefaultsForDevOutputAndCoverage
    - TestLoadInvalidBuildTarget
    - TestLoadInvalidNotesFilePath
    - TestLoadLibraryModeConfig
    - TestLoadRejectsInvalidDevOutputStyle
    - TestLoadRejectsLibraryModeWithArtifactsEnabled
    - TestLoadRejectsUnknownBuildField
    - TestLoadRejectsUnknownTopLevelField
    - TestLoadValidConfig
  import_files_list:
    - os
    - path/filepath
    - strings
    - testing
  language: go
  purpose: Guard config loading and validation contracts so cue-backed project/build/release behavior remains deterministic.
  responsibilities: Check valid parsing defaults and reject invalid field combinations, enums, and required-field omissions.
