locator: internal/config/types.go
meta:
  architecture_notes: Feature `*Set` booleans preserve explicitly-configured semantics so unset fields are not treated as forced false during reconciliation.
  function_list: []
  import_files_list:
    - regexp
  language: go
  purpose: Define the canonical in-memory schema for gh-flarebyte configuration.
  responsibilities: Declare config structs/constants; model repository/build/release fields and feature-presence flags used by planning logic.
