locator: internal/cli/build_helpers.go
meta:
  architecture_notes: External process interactions are exposed through package variables for deterministic test stubbing while keeping production behavior shell-based.
  function_list:
    - buildGoEnv
    - copyFile
    - findCGODependencies
    - goBuildError
    - goBuildTargetBinary
    - hydrateBuildInfo
    - packageBinaryArchive
    - packageTarGz
    - packageZip
    - resolveChecksumPath
    - runGoBuildPackages
    - sha256File
    - splitTarget
    - zeroTime
  import_files_list:
    - archive/tar
    - archive/zip
    - bytes
    - compress/gzip
    - crypto/sha256
    - fmt
    - github.com/flarebyte/gh-flarebyte/internal/buildinfo
    - github.com/flarebyte/gh-flarebyte/internal/config
    - io
    - os
    - os/exec
    - path
    - path/filepath
    - runtime
    - strings
    - time
  language: go
  purpose: Provide reusable build primitives for compiling, packaging, hashing, and build-metadata hydration.
  responsibilities: Parse build args; compile per-target binaries; package archives; compute checksums; copy artifacts; populate ldflags metadata inputs.
