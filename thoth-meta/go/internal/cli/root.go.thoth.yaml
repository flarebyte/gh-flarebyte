locator: internal/cli/root.go
meta:
  architecture_notes: Command handlers are wired through package-level function variables to keep command orchestration testable with lightweight stubs.
  function_list:
    - Run
    - contains
    - currentVersionInfo
    - printHelp
  import_files_list:
    - github.com/flarebyte/gh-flarebyte/internal/buildinfo
    - io
    - os
    - runtime
  language: go
  purpose: Define the CLI domain model and top-level command router for gh-flarebyte.
  responsibilities: Declare shared CLI types/exit codes; dispatch subcommands; render help/version output.
