locator: internal/cli/cmd_release.go
meta:
  architecture_notes: Release always shells through the build command first so artifact shape and checksum behavior stay consistent with standalone build execution.
  function_list:
    - runRelease
  import_files_list:
    - fmt
    - io
  language: go
  purpose: Implement release publishing by composing build outputs, version resolution, and GitHub release creation.
  responsibilities: Parse release flags; trigger build; resolve tag/version; collect artifacts; create release with selected notes mode.
