locator: internal/cli/exec_helpers.go
meta:
  architecture_notes: This keeps shell-backed helpers behaviorally consistent across commands and avoids leaking noisy wrapped exec details.
  function_list:
    - commandError
  import_files_list:
    - errors
    - strings
  language: go
  purpose: Normalize stderr-first command execution failures into user-facing error messages.
  responsibilities: Convert exec errors into concise errors; prefer captured stderr text over generic process errors.
