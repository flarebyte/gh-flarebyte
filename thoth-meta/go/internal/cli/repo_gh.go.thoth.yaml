locator: internal/cli/repo_gh.go
meta:
  architecture_notes: repositoryTopics decoding accepts both connection and array shapes to tolerate upstream gh output variations without breaking audit/update.
  function_list:
    - boolToCLIValue
    - extractLabelsFromState
    - extractTopics
    - ghAddRepoTopic
    - ghApplyRepoSettings
    - ghCreateRepoLabel
    - ghDeleteRepoLabel
    - ghReadRepoMetadata
    - ghReadReposMine
    - ghRemoveRepoTopic
    - ghUpdateRepoLabel
    - runGH
    - runGHReadonly
  import_files_list:
    - bytes
    - encoding/json
    - os
    - os/exec
    - strings
  language: go
  purpose: Encapsulate GitHub CLI/API I/O for repository metadata reads and repository mutation operations.
  responsibilities: Read repo/repositories state from gh; translate JSON payloads into internal models; apply settings/topics/labels via gh commands.
