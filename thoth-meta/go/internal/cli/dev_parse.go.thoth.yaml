locator: internal/cli/dev_parse.go
meta:
  architecture_notes: Parsers are intentionally tolerant of unrelated lines so command summaries remain stable across minor tool output changes.
  function_list:
    - parseCoverageDetails
    - parseGoTestJSONSummary
    - parseGoTestReport
    - parseTotalCoverage
  import_files_list:
    - encoding/json
    - fmt
    - regexp
    - strconv
    - strings
  language: go
  purpose: Parse raw tool outputs into typed test/coverage summaries used by dev commands.
  responsibilities: Extract total coverage; parse per-entry coverage details; decode go test JSON events into structured reports.
