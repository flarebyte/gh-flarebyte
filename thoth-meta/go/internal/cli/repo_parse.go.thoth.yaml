locator: internal/cli/repo_parse.go
meta:
  function_list:
    - mustAbs
    - parseRepoAuditArgs
    - parseRepoInitArgs
    - parseRepoUpdateArgs
    - parseReposMineArgs
    - splitRepo
  import_files_list:
    - errors
    - fmt
    - path/filepath
    - strings
  language: go
  purpose: Parse repo-related CLI arguments into typed values consumed by repo command handlers.
  responsibilities: Validate repo/audit/update/mine flags; split owner/name repo strings; resolve absolute paths for messages.
  architecture_notes: Parsers are strict (unknown-arg rejection) to keep automation behavior deterministic and avoid silently ignored flags.
