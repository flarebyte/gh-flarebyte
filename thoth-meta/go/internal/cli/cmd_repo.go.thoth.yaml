locator: internal/cli/cmd_repo.go
meta:
  function_list:
    - handleRepoAudit
    - handleRepoInit
    - handleRepoUpdate
    - handleReposMine
  import_files_list:
    - encoding/json
    - fmt
    - github.com/flarebyte/gh-flarebyte/internal/config
    - io
  language: go
  purpose: Implement user-facing repository workflows (init, audit, update, mine) on top of config and GitHub adapters.
  responsibilities: Parse repo command intent; enforce safety gates; orchestrate plan execution; format repo workflow output.
  architecture_notes: Update applies settings/topics/labels sequentially and intentionally avoids rollback, surfacing partial-progress failures for deterministic reruns.
