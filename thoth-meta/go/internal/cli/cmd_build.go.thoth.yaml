locator: internal/cli/cmd_build.go
meta:
  function_list:
    - handleBuild
  import_files_list:
    - fmt
    - io
    - os
    - path/filepath
    - sort
    - strings
  language: go
  purpose: Execute the configured build workflow to produce target artifacts and checksums.
  responsibilities: Validate build arguments/config; iterate targets; build/package binaries; emit checksum and summary output.
  architecture_notes: Build metadata is hydrated before compilation so produced binaries report version info even when the running extension binary was built in dev mode.
