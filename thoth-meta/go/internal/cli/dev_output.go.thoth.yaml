locator: internal/cli/dev_output.go
meta:
  architecture_notes: Output behavior is config-driven (`devOutput`) so presentation can evolve without changing command execution paths.
  function_list:
    - extractFailureLocation
    - formatFailureDetails
    - formatGoTestDetails
    - printCoverageDetails
    - printDevSummary
    - printPerTestEvents
    - useColor
  import_files_list:
    - fmt
    - github.com/flarebyte/gh-flarebyte/internal/config
    - io
    - os
    - regexp
    - strings
    - time
  language: go
  purpose: Render dev-command output in consistent summary and per-test/per-entry formats.
  responsibilities: Format status lines; apply color policy; print test and coverage entries; format failure snippets.
