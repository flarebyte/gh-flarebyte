locator: internal/cli/release_helpers.go
meta:
  architecture_notes: Artifact matching intentionally supports both flat and per-target layouts so build.artifactTargetSuffix modes can share one release path.
  function_list:
    - ensureUniqueArtifactBasenames
    - ghCreateRelease
    - ghTagExists
    - isSemver
    - listReleaseArtifacts
    - parseReleaseArgs
    - resolveVersionFromSource
    - ternary
  import_files_list:
    - bytes
    - encoding/json
    - errors
    - fmt
    - os
    - os/exec
    - path
    - path/filepath
    - regexp
    - sort
    - strings
  language: go
  purpose: Provide argument parsing, version extraction, artifact discovery, and GitHub-side primitives for release flows.
  responsibilities: Parse release flags; read semver from YAML/JSON; collect artifacts/checksums; guard duplicate names; create/check GitHub releases.
